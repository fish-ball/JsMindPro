export default class {
  // Example data.
  static example = null

  /**
   * 将制定的数据格式导入 model
   * @param model {JsMindModel}
   * @param data
   */
  static load (model, data) {
  }

  /**
   * 将 model 转换为当前的数据格式
   * @param model {JsMindModel}
   */
  static dump (model) {
  }
}
